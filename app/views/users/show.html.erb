<div class="row flex">
    <div class="card orange darken-2">
      <div class="row">
        <div class="col s2">
          <i class="large material-icons amber-text">person</i>
        </div>
        <div class="col s10">
          <h3 class="white-text"><%= @user.nickname %>'s TAG</h3>
        </div>
      </div>
    </div>

<div class="row">
  <%@tweets.each do |tweet|%>
   <%= render partial: "tweets/tweets-2" , locals: { tweet: tweet }%>
  <%end%>
</div>
<%= paginate @tweets %>
